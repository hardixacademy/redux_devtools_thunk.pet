const defaultState = {
    cash: 0
};

export const homeReducer = (state = defaultState, action) => {
    switch (action.type) {
        case value:

            break;

        default:
            return state
    }
}